// import { AUDIO } from '../constants/ActionTypes'

const initialState = {
}

export default function counter (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
